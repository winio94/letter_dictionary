Plsease run following maven command from project main directry in order to build project, execute unit and mutation tests:

```mvn clean package org.pitest:pitest-maven:mutationCoverage -s=settings.xml```
